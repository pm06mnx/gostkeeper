<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.mnx.gostkeeper">

    <application
        android:name=".GostKeeperApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SecretsListActivity"
            android:label="@string/secrets_list_activity_name">
        </activity>
        <activity
            android:name=".ViewSecretActivity"
            android:label="@string/view_secret_activity_name"/>
        <activity
            android:name=".CreateSecretActivity"
            android:label="@string/create_secret_activity_name"/>
        <activity
            android:name=".PasswordActivity"
            android:label="@string/password_activity_name"
            android:windowSoftInputMode="stateAlwaysVisible"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
